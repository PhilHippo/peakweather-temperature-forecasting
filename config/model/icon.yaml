# @package _global_

#### Model params #############################################################
# ICON NWP baseline - uses ICON-CH1-EPS ensemble forecasts
model:
  name: icon
  hparams: {} 

# Force probabilistic evaluation for ICON (Energy Score requires samples)
loss_fn: ens

# ICON provides its own test set evaluation
nwp_test_set: true
